
# 📊 ScheduleFlow 대시보드 화면 구성

## 전체 레이아웃 개요

┌────────────────────────────────────────────┐
│                   Header                   │
├───────┬────────────────────────────────────┤         
│Sidebar│                    │ My Tasks      │
│       │                    │ (Priority)    │    
│       │     Calendar       │───────────────┤
│       │                    │               │
│       │                    │ Team Status   │
└───────┴────────────────────┴───────────────┘



## 화면 구성 세부 설명

### 🧭 헤더(전역)
- 로고, 사용자 프로필
- 프로젝트 전반 공통 액션 및 정보 접근
- 사용자 프로필 드롭다운: 내 정보 / 로그아웃

### 📁 사이드바(전역)
- **내용:** 메뉴 목록 (대시보드 / 프로젝트 / 파일 / 일정 / 거래처 / 사원)
- **특징:** 현재 페이지 하이라이트 표시

### 📅 캘린더 영역
중앙 좌측 40%
📅 2025.12 [◀] [프로젝트◉][프로+일정○][기본○] [▶]
Su Mo Tu We Th Fr Sa
├─────────────────────────────┤
│  1  2  3  4  5  6  7        │
│█BOM 설계 ────────▶10        │ ← 프로젝트 바
│  8✓BOM 검토 10 11 12 13 14  │ ← 프로젝트+일정
│█PCB 설계 ────────▶25        │
│15 16 17 18 19 20✓PCB리뷰 21 │
│22 23 24 25 26●휴가 27 28    │ ← 기본일정
│29 30 31                    │
└─────────────────────────────┘


- **기본 모드:** 월간 일정 보기 (Month View)
- **토글 모드:**
    1. **프로젝트**
        - 디폴트 모드
        - 해당 월에 속한 프로젝트를 불러옴
        - 프로젝트 이름과 지정된 `color`로 바(bar) 형태 표시
    2. **프로젝트 + 일정**
        - 프로젝트 정보 + 해당 프로젝트에 속한 일정(마감, 회의 등)을 함께 표시
    3. **기본 일정**
        - 프로젝트에 속하지 않은 개인 일정만 표시

- **표시 방식:**
    - 캘린더 셀 내에서 **프로젝트/일정 바 형태로 시각화**
    - 동일한 날짜에 여러 항목이 있을 경우, **바 아래에 순서대로 나열**

- **상호작용:**
    - 프로젝트 또는 일정을 클릭하면 **상세 정보 모달** 표시
        - 프로젝트: 진행 기간, 담당자, 관련 파일, 상태 등
        - 일정: 제목, 시간, 관련 프로젝트(있다면), 메모 등

- **기본 디스플레이 흐름:**
    - 달력 상단 헤더에서 토글 선택 → 해당 데이터 모드로 캘린더 갱신
    - 오늘 날짜 강조 표시
    - Hover 시 짧은 요약 툴팁, 클릭 시 상세 모달



### **My Tasks 영역**
중앙 우측 상단 30%
✅ 이번달 내 할 일 (8)
├─────────────────────────────────────┤
│ • BOM 검토 27일 14:00 ⏰3h [P]BOM  │ ← 마감 급한순
│ • PCB 리뷰 25일 ⚡4d [P]PCB        │
│ • 부품 발주 28일 [P]BOM            │
│ • 최종 검수 31일 [P]PCB            │
│ ... +4 더보기                      │
└─────────────────────────────────────┘


- 이번달에 나한테 할당된 일만 표시(로그인한 사용자 기반)
- 마감일 급한순 정렬
- 최대 8개 표시 + "... +N 더보기"
- Task 클릭 → 캘린더와 동일 모달


### 👥 팀원 오늘 일정
중앙 우측 하단 30%
👥 팀원 오늘 일정
├─────────────────────────────────────┤
│ 김팀장     │ BOM 검토 14:00        │
│ 이사원     │ PCB 스키마 수정       │
│ 박주임     │ 부품 발주 대기        │
│ 최인턴     │ -                    │
│           +2명 더보기              │
└─────────────────────────────────────┘

- 모든 팀원 오늘 일정 확인

---

TODO 리스트 구조
1. 헤더 관련 (3개)
Header 컴포넌트 구현
프로필 드롭다운 메뉴 (내 정보 / 로그아웃)
로그아웃 기능 구현
2. 사이드바 관련 (2개)
Sidebar 컴포넌트 구현 (메뉴 목록)
현재 페이지 하이라이트 표시
3. 레이아웃 통합 (2개)
레이아웃 래퍼 컴포넌트 (Header + Sidebar + Content)
보호된 라우트에 레이아웃 적용
4. 대시보드 기본 구조 (1개)
대시보드 페이지 레이아웃 (캘린더 40% + 우측 60%)
5. 캘린더 관련 (3개)
캘린더 기본 UI (월간 보기, 날짜 그리드)
토글 모드 UI (프로젝트/프로젝트+일정/기본일정)
오늘 날짜 강조 표시
6. My Tasks 관련 (2개)
My Tasks 컴포넌트 기본 UI
최대 8개 표시 + 더보기 기능
7. 팀원 일정 (1개)
팀원 오늘 일정 컴포넌트 기본 UI
총 14개의 TODO 항목으로 구성했습니다. 어떤 순서로 진행할까요?


### 켈린더
**직접 구현하는 게 가장 빠르고 맞춤형입니다.**[1][2] 기존 라이브러리는 **"프로젝트 바(█) + 토글 모드"** 커스텀을 하려면 오히려 더 복잡해집니다.

## ❌ **라이브러리 단점 (ScheduleFlow엔 부적합)**

| 라이브러리 | 문제점 |
|------------|--------|
| `react-big-calendar` | 이벤트만, 프로젝트 바 불가 [3] |
| `react-calendar-timeline` | 가로 타임라인, 월간X [4] |
| `FullCalendar` | 무거움(200KB+), 커스텀 어려움 |
| `Syncfusion`/`DevExtreme` | 상용(유료), 번들 크기 큼 [5][6] |

## ✅ **추천: Custom CSS Grid (2시간 완성)**

```jsx
// 40줄로 월간 캘린더 완성
const CalendarGrid = ({ data, mode }) => (
  <div className="grid grid-cols-7 gap-px bg-gray-200 h-96 rounded-lg overflow-hidden">
    {days.map(day => (
      <div key={day.date} className="bg-white p-1 relative h-24 border">
        {/* 프로젝트 바 */}
        {mode === 'PROJECT' && day.projects.map(p => (
          <div 
            key={p.id}
            className="project-bar h-2 rounded"
            style={{ backgroundColor: p.color }}
          />
        ))}
        {/* 일정 */}
        {mode === 'PROJECT_TODO' && day.projectTodos.map(t => (
          <div key={t.id} className="text-xs">✓{t.title}</div>
        ))}
        <span className="absolute bottom-1 right-1 text-xs font-bold">
          {day.date.day}
        </span>
      </div>
    ))}
  </div>
);
```

## 🎯 **왜 Custom이 최적인가**

```
✅ Tailwind Grid: 5줄로 월간 레이아웃
✅ 프로젝트 바: position:absolute 3줄
✅ 토글 모드: 조건부 렌더링 if문
✅ 번들: 2KB vs 라이브러리 200KB+
✅ 유지보수: ScheduleFlow 전용
```

## 🚀 **구현 템플릿 (복사해서 바로 사용)**

```jsx
// CalendarSection.jsx - 완전 구현
import { useState, useEffect } from 'react';

const CalendarSection = () => {
  const [data, setData] = useState(null);
  const [mode, setMode] = useState('PROJECT');
  
  useEffect(() => {
    fetch(`/api/calendar/monthly?mode=${mode}`)
      .then(res => res.json())
      .then(setData);
  }, [mode]);
  
  const modes = [
    { id: 'PROJECT', label: '프로젝트' },
    { id: 'PROJECT_TODO', label: '프로+일정' },
    { id: 'BASE_TODO', label: '기본일정' }
  ];
  
  return (
    <div className="bg-white rounded-lg shadow p-6">
      <div className="flex justify-between items-center mb-4">
        <h3 className="text-xl font-bold">📅 2025.12</h3>
        <div className="flex gap-2">
          {modes.map(m => (
            <button
              key={m.id}
              onClick={() => setMode(m.id)}
              className={`px-3 py-1 rounded-full text-sm ${
                mode === m.id ? 'bg-blue-500 text-white' : 'bg-gray-100'
              }`}
            >
              {m.label}
            </button>
          ))}
        </div>
      </div>
      
      {/* CSS Grid 캘린더 */}
      <div className="grid grid-cols-7 gap-px bg-gray-200 rounded-lg overflow-hidden">
        {['Su','Mo','Tu','We','Th','Fr','Sa'].map(day => (
          <div key={day} className="bg-gray-300 py-2 text-center font-medium text-xs">
            {day}
          </div>
        ))}
        {/* 데이터 렌더링은 위 예시처럼 */}
      </div>
    </div>
  );
};
```

## 📦 **패키지 추가 필요?**

```
npm i date-fns  // 날짜 계산만 (3KB)
```


