### 회원가입 테스트
POST http://localhost:8080/auth/sign-up
Content-Type: application/json

{
  "username": "admin",
  "password": "password123",
  "name" : "김성현",
  "email" : "test@example.com",
  "phone": "010-1234-5678"
}

### 로그인 테스트 (성공시 토큰 발급)
POST http://localhost:8080/auth/sign-in
Content-Type: application/json

{
  "username": "admin",
  "password": "password123"
}

> {%
    // 로그인 성공 후 토큰을 변수에 저장
    client.global.set("auth_token", response.body);
%}

### ADMIN 권한 테스트 - 삭제 API (토큰 필요)
DELETE http://localhost:8080/users/
Authorization: Bearer {{auth_token}}
Content-Type: application/json


### ============================================
### 프로젝트 API
### ============================================

### 프로젝트 목록 조회
GET http://localhost:8080/projects
Authorization: Bearer {{auth_token}}
Content-Type: application/json

### 프로젝트 상세 조회
GET http://localhost:8080/projects/1
Authorization: Bearer {{auth_token}}
Content-Type: application/json

### 프로젝트 생성
POST http://localhost:8080/projects
Authorization: Bearer {{auth_token}}
Content-Type: application/json

{
  "name": "스크류바 카톤",
  "clientId": 1,
  "memberIds": [1, 2],
  "partnerContactIds": [1,2],
  "status": "IN_PROGRESS",
  "startDate": "2024-01-01",
  "endDate": "2024-12-31",
  "description": "프로젝트 설명",
  "colorCode": "#FF5733"
}

> {%
    // 프로젝트 생성 성공 후 ID를 변수에 저장
    client.global.set("project_id", response.body);
%}

### 프로젝트 수정 (Controller에 구현 필요)
PUT http://localhost:8080/projects/{{project_id}}
Authorization: Bearer {{auth_token}}
Content-Type: application/json

{
  "name": "수정된 프로젝트명",
  "description": "수정된 설명",
  "colorCode": "#00FF00",
  "status": "COMPLETED"
}

### 프로젝트 멤버 변경 (Controller에 구현 필요)
PUT http://localhost:8080/projects/{{project_id}}
Authorization: Bearer {{auth_token}}
Content-Type: application/json

{
  "memberIds": [1, 2, 3]
}

### 프로젝트 삭제 (Controller에 구현 필요)
DELETE http://localhost:8080/projects/{{project_id}}
Authorization: Bearer {{auth_token}}
Content-Type: application/json
